{
  "rules": {
    ".read": false,
    ".write": false,

    "presence": {
      "$uid": {
        ".read":  "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "trackerCandidates": {
      "$busId": {
        // Anyone authenticated can see who is on a bus to calculate centroids
        ".read": "auth != null",
        "$uid": {
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    },

    "approachingStudents": {
      "$busId": {
        // Anyone authenticated can see who is waiting to enable mutual discovery
        ".read": "auth != null",
        "$uid": {
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    },

    "trackerAssignments": {
      "$busId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },

    "busLocations": {
      "$busId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },

    "busHealth": {
      "$busId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },

    "sessions": {
      "$uid": {
        ".read":  "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
